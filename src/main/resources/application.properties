spring.application.name=flightapi

# ? Database Configuration
spring.datasource.url=jdbc:mysql://flight-api-database.cd06isyk8blt.us-east-2.rds.amazonaws.com:3306/flightdb?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=true&verifyServerCertificate=false&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=student
spring.datasource.username=student
spring.datasource.password=Keyin2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? Hibernate / JPA Config
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Server port (optional)
server.port=8080

# ? Optional - prevents Jackson errors with bi-directional relationships
spring.jackson.serialization.fail-on-empty-beans=false


management.endpoints.web.exposure.include=health
management.endpoint.health.probes.enabled=true
management.endpoint.health.show-details=never

